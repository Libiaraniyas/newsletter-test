[build]
  publish = "."
  command = ""
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  AWS_LAMBDA_JS_RUNTIME = "nodejs18.x"

[functions]
  directory = "netlify/functions"

[functions."fetch-news"]
  node_bundler = "esbuild"

[functions."fetch-benchmark"]
  node_bundler = "esbuild"
